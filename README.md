# Port-Scanner
"A multi-threaded network scanner built in Python to perform fast host and port discovery. It features built-in proxy rotation (including Tor support) to prevent firewall blocking, captures application banner details to identify running services, and checks packet signatures to determine the target's operating system.
